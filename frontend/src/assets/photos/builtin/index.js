// Built-in photos for PWA standalone mode.
// Currently empty — add photos to this folder and update imports when ready.
// Shape must match PhotoManager: { id, src, name }

const builtinPhotos = [];

export default builtinPhotos;
